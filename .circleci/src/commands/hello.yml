#
# Hello
#
description: Hello
steps:
  - run:
      name: Hello
      command: |
        #!/usr/bin/env sh
        set -eu
        echo "pwd=$(pwd)"
        echo "whoami=$(whoami)"
        echo 'Hello'
