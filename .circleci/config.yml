version: 2.1
setup: true

jobs:
  setup:
    steps:
      - checkout
      - run:
          name: hello
          command: |
            #!/usr/bin/env sh
            set -eu
            echo 'Hello'
